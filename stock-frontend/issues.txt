organize the api url before | fixed

-- page articles :
- restore does not remove search | fixed

--page chantiers :
- chantier summary still uses old backend endpoint (localhost:4000) | fixed

-- page demandes articles :
- export fiche expedition does not work | fixed

-- page entrée confirme :
- entrée details does not load on the modal (probably linked to relationship errors) | fixed
- list articles does not load articles (probably linked to relationship errors) | fixed

-- page sortie confirmes :
- export bon de livraison does not work | fixed


--known issues :
- demande articles does not appear on the page, possibly retours too
- 503 error, server is busy (happened when confirming a sortie)
- relationship bugs | fixed
- some document based operations use a static localhost backend url, will cause an issue in production | fixed

-- backend checks :
- Number() without verifying NaN
- missing joi validators
- multipart forms are sent empty and rejected by the validator (ajouter entree)
- all routes divert into the global error handler if an error occures

-- Frontend checks :
- happy paths
- edge cases
- null relationship behaviour
- loading indicators and buttons disabled
- uses dynamic api url